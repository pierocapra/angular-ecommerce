import { ShopCityValidators } from './shop-city-validators';

describe('ShopCityValidators', () => {
  it('should create an instance', () => {
    expect(new ShopCityValidators()).toBeTruthy();
  });
});
